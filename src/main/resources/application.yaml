server:
  port: 8080

logging:
  level:
    org.springframework.web: DEBUG
    org.springframework.messaging: DEBUG

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/iot"
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    show_sql: true
mqtt:
  broker:
    url: "tcp://localhost:1885"
  client:
    id: "hoanghung15"
  username: "hoanghung15"
  password: "12345"
  topic: "esp/control"